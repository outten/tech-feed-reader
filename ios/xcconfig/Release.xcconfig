// Production API — Phase 1 (recovery-code auth + browser-handoff sign-up)
// doesn't need Associated Domains to work; those are still Phase 2
// (native passkey UI) — see openspec/changes/ios-app/design.md.
API_BASE_URL = https:/$()/feeder.tmoneystuff.com
