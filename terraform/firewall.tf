# DigitalOcean cloud firewall — operates at the network edge, before
# packets reach the Droplet. Belt-and-suspenders alongside any UFW
# rules inside the box.
#
# Inbound: 22 (SSH) only from configured CIDRs; 80 + 443 from anywhere.
# Outbound: any (Droplet needs to reach Anthropic API, apt mirrors,
# Picsum for backgrounds, DO Spaces, etc).

resource "digitalocean_firewall" "app" {
  name = "tech-feed-reader-fw"

  droplet_ids = [digitalocean_droplet.app.id]

  inbound_rule {
    protocol         = "tcp"
    port_range       = "22"
    source_addresses = var.allowed_ssh_cidrs
  }

  inbound_rule {
    protocol         = "tcp"
    port_range       = "80"
    source_addresses = ["0.0.0.0/0", "::/0"]
  }

  inbound_rule {
    protocol         = "tcp"
    port_range       = "443"
    source_addresses = ["0.0.0.0/0", "::/0"]
  }

  # ICMP for ping / traceroute diagnostics from anywhere.
  inbound_rule {
    protocol         = "icmp"
    source_addresses = ["0.0.0.0/0", "::/0"]
  }

  outbound_rule {
    protocol              = "tcp"
    port_range            = "1-65535"
    destination_addresses = ["0.0.0.0/0", "::/0"]
  }

  outbound_rule {
    protocol              = "udp"
    port_range            = "1-65535"
    destination_addresses = ["0.0.0.0/0", "::/0"]
  }

  outbound_rule {
    protocol              = "icmp"
    destination_addresses = ["0.0.0.0/0", "::/0"]
  }
}
