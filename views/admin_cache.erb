<h2>Cache admin</h2>
<p class="subtitle">Per-feed cache age + manual refresh.</p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    Per-feed cache age, refresh-one button (synchronous), refresh-all button
    (background thread with live progress banner).  Mirrors t-money's
    <code>/admin/cache</code> page.
  </p>
</section>
