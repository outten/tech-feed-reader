<h2>Provider health</h2>
<p class="subtitle">Per-feed fetch success rate, latency, and last error.</p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    Bounded ring buffer of feed-fetch observations (status, latency, reason)
    surfaced as a sortable table. <code>HealthRegistry.degraded</code>
    drives a banner on the dashboard when fetches systemically fail.
  </p>
</section>
