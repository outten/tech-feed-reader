<h2>Article</h2>
<p class="subtitle">id: <code><%= @article_id %></code></p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    Single-article view: title · publisher · published_at · sanitized content · cached summary · tag chips.
    Renders from <code>ArticlesStore</code> + <code>SummaryStore</code> only — no on-render fetches.
  </p>
</section>
