<h2>Articles</h2>
<p class="subtitle">Main reading interface — paginated, filterable by tag / feed / read-state.</p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    The articles page renders <code>ArticlesStore</code> — sorted by recency, with
    tag chips and a per-row read-toggle. Empty until the feed fetcher lands.
  </p>
</section>
