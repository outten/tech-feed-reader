<h2>Dashboard</h2>
<p class="subtitle">
  Recent unread, top tags, feed-fetch health. Empty until feeds + articles land — see <a href="/feeds">Feeds</a> to add your first source.
</p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    The dashboard surfaces the most recent unread articles, top tags this week,
    and a feed-fetch health banner once the underlying stores ship. See
    <code>SPEC.md</code> for the planned shape.
  </p>
</section>
