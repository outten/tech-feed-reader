<h2>Tags</h2>
<p class="subtitle">Tag rules and per-tag activity.</p>

<section class="benchmark-section">
  <h3>Coming up</h3>
  <p class="data-source">
    User-defined tag rules (regex / keyword / feed_id) with auto-apply
    on fetch. Lands in Tier 2 — see <code>SPEC.md</code> milestone H.
  </p>
</section>
